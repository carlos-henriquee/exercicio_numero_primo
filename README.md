# This is a project that is part of a list of a exercises 
## There is the question right down:
### We say that a natural number n is prime if n has exactly two divisors. Note that 1 is not prime, since 1 has only one divisor. Write a program that takes an input of n and checks whether n is prime. Challenge: Modify the code so that it takes an integer k as input and prints all primes less than k.
## How to run it?
### First, clone this repository to your machine.
### If you are on Windows, then make sure you already have gcc installed. If you don't, follow this [tutorial](https://dev.to/gamegods3/how-to-install-gcc-in-windows-10-the-easier-way-422j)
Then, open cmd and navigate to your working directory:
```
cd *path/to/the/file/directory*
```
Now, run the code usign the gcc compiler:
```
gcc main.c -o "name you want for the executable"
```

### If you are on Linux, you probably already have gcc. If you don't, just run this commands on your terminal:
```
sudo apt update
sudo apt install build-essential
```
Go to the directory where the file .c is:

```
cd *path/to/the/file/directory*
```


And then, run the code:
```
gcc main.c -o "name you want for the executable"
```
